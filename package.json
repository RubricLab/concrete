{
	"devDependencies": {
		"@biomejs/biome": "^2.3.8",
		"@rubriclab/config": "^0.0.24",
		"@types/react": "^19.2.7",
		"@types/react-dom": "^19.2.3",
		"typescript": "^5.9.3"
	},
	"name": "@rubriclab/concrete-workspace",
	"private": true,
	"scripts": {
		"build": "bun run build:package && bun run build:docs",
		"build:docs": "bun run --cwd apps/docs build",
		"build:package": "bun run --cwd packages/concrete build",
		"check": "bun run build:package && bun x biome check . && bun run typecheck",
		"clean": "rm -rf node_modules apps/docs/.next packages/concrete/dist",
		"dev": "bun run build:package && bun run --cwd apps/docs dev",
		"format": "bun x biome check . --write",
		"typecheck": "bun run build:package && bun run --cwd apps/docs typecheck"
	},
	"workspaces": [
		"apps/*",
		"packages/*"
	]
}
