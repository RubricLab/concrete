{
	"devDependencies": {
		"@biomejs/biome": "^2.3.8",
		"@rubriclab/config": "*",
		"@types/bun": "latest",
		"typescript": "^5.9.3"
	},
	"name": "concrete",
	"private": true,
	"scripts": {
		"audit:imports": "bun test packages/concrete/src/tests/import-boundaries.test.ts",
		"bleed": "bun x npm-check-updates -u",
		"build": "bun run build:package && bun run build:docs",
		"build:docs": "bun run --cwd apps/docs build:app",
		"build:package": "bun run --cwd packages/concrete build",
		"build:skill": "bun run --cwd packages/concrete build:skill",
		"catalog:audit": "bun run --cwd apps/docs catalog:audit",
		"check": "bun run typecheck && bun run lint && bun run check:skill && bun test",
		"check:skill": "bun run --cwd packages/concrete check:skill",
		"clean": "rm -rf node_modules apps/docs/.next packages/concrete/.tsbuildinfo",
		"dev": "bun run build:package && bun run --cwd apps/docs dev",
		"dev:docs": "bun run --cwd apps/docs dev",
		"format": "bun x biome check . --write",
		"lint": "bun x biome check .",
		"typecheck": "bun run --cwd packages/concrete typecheck && bun run build:package && bun run --cwd apps/docs typecheck:app",
		"visual:smoke": "bun run --cwd apps/docs visual:smoke"
	},
	"workspaces": [
		"apps/*",
		"packages/*"
	]
}
