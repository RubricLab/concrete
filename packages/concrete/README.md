# @rubriclab/concrete

Concrete is a design system for labs that ship. It exports namespaced CSS tokens,
component styles, React primitives, icons, and typed documentation registries.

```tsx
import '@rubriclab/concrete/styles.css'
import { Button, primitiveRegistry } from '@rubriclab/concrete'
```
